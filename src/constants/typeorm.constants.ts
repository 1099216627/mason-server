/* 角色状态 0禁用 1启用 */
export enum UserStatus {
  ENABLED = 1,
  DISABLED = 0,
}
/* 角色性别 0男 1女 */
export enum UserGender {
  MALE = 0,
  FEMALE = 1,
}

/* 是否为预设记录 1是 0否 */
export enum IsPreset {
  YES = 1,
  NO = 0,
}
